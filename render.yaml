services:
  - type: web
    name: acme-web
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn acme.wsgi
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: acme-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: acme-redis
          type: redis
    plan: free

  - type: worker
    name: acme-worker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: celery -A acme worker -l info
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: acme-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: acme-redis
          type: redis
    plan: free

  - type: redis
    name: acme-redis
    plan: free

databases:
  - name: acme-db
    plan: free
